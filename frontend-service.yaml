apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081  # 외부에서 접근할 수 있게 고정 포트 할당 (30000~32767 범위)

